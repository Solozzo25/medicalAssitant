[build]
  publish = "dist"
  command = "npm run build"
  functions = "netlify/functions"

[dev]
  publish = "dist"
  functions = "netlify/functions"
  
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
